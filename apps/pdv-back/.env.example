DATABASE_URL=postgres://syscontrol:syscontrol@localhost:54329/syscontrol
SERVER_PORT=3333
SERVER_TOKEN_SECRET=troque-este-segredo-em-producao
PLATFORM_ADMIN_EMAIL=admin@syscontrol.local
PLATFORM_ADMIN_PASSWORD=admin123
PLATFORM_ADMIN_NAME=Administrador
BILLING_GRACE_DAYS=5

# --- Producao (NODE_ENV=production exige segredos fortes) ---
# NODE_ENV=production
# CORS_ORIGINS=https://admin.suaempresa.com   # separadas por virgula; vazio = "*" (dev)
# ENABLE_DOCS=0                                # /docs e /openapi.json (padrao: on em dev, off em prod)
# AUTH_RATE_LIMIT_MAX=20                       # tentativas de login/join por janela
# AUTH_RATE_LIMIT_WINDOW_MS=900000            # janela do rate limit (15 min)
