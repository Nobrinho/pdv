# Ambiente de DESENVOLVIMENTO do app web/Electron (pdv).
# Carregado automaticamente pelo Vite em `npm run dev` (modo development).
# Só variáveis com prefixo VITE_ chegam ao bundle — nunca coloque segredos aqui.
VITE_API_URL=https://pdv-api-staging.onrender.com

