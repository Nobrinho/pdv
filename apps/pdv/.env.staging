# Ambiente de DEV ONLINE (homologação) do app web/Electron (pdv).
# Usado por `npm run build:staging` (Vite --mode staging).
# Aponta para o 2º backend na Render (o de dev), NÃO o de produção.
# TODO: troque pela URL real do serviço de dev quando criá-lo na Render.
VITE_API_URL=https://pdv-api-staging.onrender.com
