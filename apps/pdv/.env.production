# Ambiente de PRODUÇÃO do app web/Electron (pdv).
# Carregado automaticamente pelo Vite em `npm run build` (modo production).
# É uma URL pública (o navegador precisa dela) — não é segredo.
VITE_API_URL=https://pdv-w4es.onrender.com
