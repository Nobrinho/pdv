# Apps

Organizacao dos produtos do SysControl:

- `pdv`: aplicativo desktop Electron + React usado pelas lojas.
- `pdv-back`: API Node.js + PostgreSQL multi-loja.
- `pdv-admin`: painel web administrativo da plataforma, reservado para a proxima etapa.
