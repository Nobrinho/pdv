# Ambiente de DESENVOLVIMENTO do painel admin.
# Carregado pelo Vite em `npm run admin:dev` (modo development).
# Só variáveis VITE_ chegam ao bundle — nunca coloque segredos aqui.
VITE_API_URL=http://localhost:3333
