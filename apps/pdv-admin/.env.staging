# Ambiente de DEV ONLINE (homologação) do painel admin.
# Usado por `npm run admin:build:staging` (Vite --mode staging).
# Aponta para o backend de dev na Render, NÃO o de produção.
# TODO: troque pela URL real do serviço de dev quando criá-lo na Render.
VITE_API_URL=https://TROQUE-URL-DEV.onrender.com
