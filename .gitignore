# Dependências do Node (nunca enviar)
node_modules/

# Pastas de Build e Distribuição (onde ficam os .exe)
dist/
dist_electron/
release/
build/

# Banco de dados local (não enviar dados reais)
*.sqlite
*.sqlite3
*.db
*.db-journal

# Arquivos de log e sistema
*.log
.DS_Store
Thumbs.db

# Arquivos de ambiente local (chaves secretas)
.env
.env.local